---
name: ONDA FAME
description: Тёмный технический пульт музыкального маркетинга — чёрное поле, сетка измерения, один неоново-зелёный сигнал.
colors:
  accent: '#4ee35c'
  background: '#000000'
  surface: '#101010'
  foreground: '#ffffff'
typography:
  display: Inter
  body: Inter
rounded: '1.25rem'
---

# Design System: ONDA FAME

## Overview

**Creative North Star: «Осциллограф в тёмной студии»**

Интерфейс ведёт себя как измерительный прибор, а не как рекламный буклет. Фон — настоящий чёрный,
поверх него лежит миллиметровая сетка: она не украшение, а система координат, в которой измеряется
рост артиста. Всё, что светится зелёным, — это сигнал: цифра, подтверждённый факт, кривая роста,
целевое действие. Всё остальное — приборная панель, намеренно тихая.

Плотность средняя и ритмичная: широкие тёмные карточки с мягким скруглением, много воздуха между
секциями, короткие строки. Тон обращения — на «ты», поэтому типографика крупная и прямая, без
курсивов, без засечек, без декоративных виньеток. Драматургия строится на одном приёме: белое
утверждение, у которого вторая половина становится зелёной. Это единственный способ выделения текста
в системе.

Подтверждённые отказы: светлая тема отсутствует как класс — сайт существует только в тёмной. Ни один
цвет кроме зелёного не получает акцентной роли (синий существует только как вторая серия на графике,
где нужны две различимые линии).

**Key Characteristics:**

- Чёрный холст с технической сеткой, гаснущей к краям радиальной маской.
- Один акцент — неоново-зелёный — как обозначение сигнала, а не как украшение.
- Крупный жирный гротеск, ключевая фраза разрезана на белую и зелёную половины.
- Скруглённые тёмные карточки с волосяной границей, без вложенных карточек.
- Кнопка действия всегда одна и та же: зелёная таблетка с иконкой отправки.

## Colors

Палитра почти монохромна: чёрный, четыре ступени серого и один сигнальный зелёный.

### Primary

- **Signal Green** (`#4ee35c`): целевое действие, вторая половина ключевых заголовков, иконки услуг,
  цифры метрик, активный вопрос F.A.Q., линия роста слушателей и её значение в тултипе, кольцо
  активной стрелки карусели. Заливка кнопки — тот же цвет, текст на ней — чистый чёрный.
- **Signal Green Glow** (`rgba(78, 227, 92, 0.28)`): свечение под иконками услуг и под кнопкой при
  наведении. Всегда со смещением по Y, никогда как ровный ореол.

### Secondary

- **Data Blue** (`#3b82f6`): существует ровно в одном месте — вторая серия линейного графика в
  кейсах и её значение в тултипе, где нужны две различимые кривые. Вне графиков синий не
  используется никогда.

### Neutral

- **Void** (`#000000`): фон страницы и фон кнопочного текста на зелёном.
- **Panel** (`#101010`): заливка карточек, шапки, полей аккордеона.
- **Panel Raised** (`#161616`): вложенные плитки внутри карточки (иконка услуги, чип метрики).
- **Hairline** (`rgba(255, 255, 255, 0.08)`): все границы. Толщина всегда 1px.
- **Grid Line** (`rgba(255, 255, 255, 0.045)`): линии фоновой сетки. Внутри canvas графика
  используется чуть более плотный `--grid-line-chart` (`0.06`) — иначе линии на нём не читаются.
- **Foreground** (`#ffffff`): заголовки и имена.
- **Muted** (`#a1a1aa`): основной текст абзацев. Контраст на `#000` — 8.9:1.
- **Faint** (`#8f8f99`): подписи под метриками, «Ответим в течение рабочего дня», подписи осей
  графика. Значение подобрано по контрасту: на `#161616` даёт 5.6:1, на `#000` — 6.6:1. Более
  тёмный серый (`#71717a`) проваливал AA на приподнятой панели и был заменён по итогам аудита.

### Named Rules

**The Signal Rule.** Зелёный занимает не более 10% площади экрана. Если на одном экране больше одной
зелёной плоскости кроме кнопки — это ошибка, а не акцент.

**The Split Headline Rule.** Ключевые заголовки страницы разрезаются на белую и зелёную части по
границе смысла: белым — обстоятельство, зелёным — обещание. Ровно одна такая пара на секцию.

## Typography

**Display Font:** Inter (fallback: `system-ui, -apple-system, 'Segoe UI', Arial, sans-serif`)
**Body Font:** Inter (тот же гротеск, другие веса)
**Label/Mono Font:** отсутствует — моноширинного в системе нет.

**Character:** Один нейтральный гротеск во всех ролях. Иерархия строится весом и размером, а не
сменой гарнитуры. Кириллица — родная, не подстановочная: подключаются оба сабсета `latin` и
`cyrillic`.

### Hierarchy

- **Display** (800, 1.02–1.06, tracking `-0.04em`): H1 первого экрана — `clamp(2.75rem, 8vw, 4.5rem)`;
  заголовки CTA-блоков на ступень тише — `clamp(2rem, 5.5vw, 3.5rem)`, чтобы первый экран остался
  единственной вершиной. Потолок — 4.5rem, выше начинает распадаться на мобильном переносе.
- **Headline** (800, `clamp(1.75rem, 4vw, 2.75rem)`, 1.1, tracking `-0.03em`): заголовки секций
  («Чем мы занимаемся», «F. A.Q»).
- **Title** (700, `1.125rem`, 1.3, tracking `-0.01em`): названия услуг и имена в команде. Вопросы
  F.A.Q. на ступень мельче (`1rem`, от 640px — `1.0625rem`): это интерактивная строка, а не заголовок.
- **Body** (400, `1rem`/`1.0625rem`, 1.6): абзацы описаний. Длина строки 56–70ch — центрированный лид
  первого экрана держится у нижней границы, текст кейса и ответы F.A.Q. — у верхней.
- **Label** (500, `0.8125rem`, 1.4): подписи под метриками и ролями в команде. Регистр обычный.

### Named Rules

**The No Eyebrow Rule.** Над заголовками секций не ставится трекинговый капслок-надзаголовок.
Единственный элемент в верхнем регистре на сайте — слово FAME в логотипе.

## Layout

Контейнер — `min(100% - 2rem, 1200px)`, по центру, боковой отступ 1rem на мобильном и 1.5rem от
`640px`. Вертикальный ритм секций: `clamp(4.5rem, 9vw, 7.5rem)` сверху и снизу.

Спейсинг-шкала кратна 4px, рабочие значения: 4, 8, 12, 16, 24, 32, 48, 64, 96. Над заголовком всегда
больше воздуха, чем под ним: заголовок отделён от предыдущего блока `48px+`, а от собственного
подзаголовка — `12px`.

Сетки: услуги — 1 колонка до `640px`, 2 до `1024px`, 3 выше. Команда — 2 колонки до `768px`, 4 выше.
Кейс — одна колонка до `1024px` (текст, затем график), две от `1024px` (текст слева, график справа).
Карусели на узких экранах превращаются в горизонтальный скролл со `scroll-snap`, стрелки остаются
дублирующим, а не единственным способом навигации.

## Elevation & Depth

Система тональная, а не теневая. Глубина читается ступенями заливки `#000 → #101010 → #161616` и
волосяной границей, а не отрывом от плоскости. Тени используются ровно в двух местах — под зелёной
кнопкой и под иконкой услуги — и всегда имеют смещение по Y плюс мягкое размытие
(`0 8px 24px -8px`). Ореол без смещения запрещён.

Фоновая сетка живёт под контентом и гасится радиальной маской от центра к краям — это создаёт
ощущение подсвеченного участка приборной панели без единого градиента в контенте.

## Shapes

Скругления: карточки и панели — `20px`; вложенные плитки и чипы — `14px`; кнопки и круглые стрелки —
полная таблетка/окружность. Аватары команды — `20px` (скруглённый квадрат), не круг. Аватар артиста в
кейсе — круг, потому что это аватар профиля, а не портрет в сетке.

## Do's and Don'ts

**Do:**

- Ставить рядом с обещанием измеримую цифру: 140M+, 1.5M+, 100K+.
- Прятать стрелки карусели целиком, когда лента помещается на экран: мёртвая кнопка хуже её
  отсутствия. Место в разметке при этом сохраняется, чтобы заголовок не прыгал после гидратации.
- Держать все CTA идентичными по формулировке и назначению: «Связаться с нами» → Telegram.
- Давать анимации один авторский момент на секцию и уважать `prefers-reduced-motion`.

**Don't:**

- Не вводить светлую тему и не завязывать цвета на `prefers-color-scheme`.
- Не делать градиентный текст: выделение — только весом, размером и зелёным.
- Не вкладывать карточку в карточку; вложенный уровень — плитка без собственной границы.
- Не заменять авторский контент стоковыми иконками-заглушками в местах, где нужен реальный портрет.
- Не использовать синий нигде, кроме второй серии графика.
