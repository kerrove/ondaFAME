# Сборка и публикация статического экспорта Next.js на GitHub Pages.
#
# Проект живёт на bun.lock, поэтому зависимости ставит Bun. Саму сборку запускает настоящий Node:
# скрипт build вызывает node напрямую (см. package.json и CLAUDE.md) — под `bun run` Bun исполнял бы
# бинарник next внутри себя и падал на завершении процесса.
#
# Статический экспорт включается только здесь, через GITHUB_PAGES=true: постоянно держать
# output: 'export' нельзя, иначе перестанут работать next dev и next start.
name: Deploy Next.js site to Pages

on:
  push:
    branches: ['main']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Не отменяем идущий деплой, но лишние очереди схлопываем
concurrency:
  group: 'pages'
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.8

      # Кеш зависимостей у setup-node НЕ включаем: он умеет только package-lock.json,
      # npm-shrinkwrap.json и yarn.lock, а у проекта bun.lock — на этом workflow и падал.
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v6

      - name: Restore Next.js cache
        uses: actions/cache@v4
        with:
          path: .next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('bun.lock') }}-${{ hashFiles('src/**/*.[jt]s', 'src/**/*.[jt]sx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('bun.lock') }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # .env в репозиторий не коммитится, поэтому в CI его нет: значения приходят секретами
      # репозитория (Settings → Secrets and variables → Actions). Незаданный секрет разворачивается
      # в пустую строку, поэтому у каждого есть запасной вариант — сборка не падает без настройки.
      - name: Build static site
        env:
          GITHUB_PAGES: 'true'
          # На Pages сайт живёт в подкаталоге /<repo>, без basePath ассеты уедут в корень домена.
          # Читается и next.config.ts (basePath), и кодом приложения (src/utils/asset.ts).
          NEXT_PUBLIC_BASE_PATH: ${{ steps.pages.outputs.base_path }}
          # Секрет SITE_URL — боевой домен. Пока его нет, берётся адрес самих Pages.
          # Читается в src/constants/site.ts — canonical, sitemap.xml, robots.txt, og:url.
          # Свой домен требует настройки и в самом Pages: base_path приходит оттуда, и если
          # домен прописан только в секрете, ссылки уедут в подкаталог /<repo>.
          NEXT_PUBLIC_SITE_URL: ${{ secrets.SITE_URL || format('{0}{1}', steps.pages.outputs.origin, steps.pages.outputs.base_path) }}
          # Секрет TG_URL — адрес, куда ведут все кнопки «Связаться с нами».
          # Пусто — остаётся значение по умолчанию из src/constants/site.ts.
          NEXT_PUBLIC_TELEGRAM_URL: ${{ secrets.TG_URL }}
        run: bun run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
